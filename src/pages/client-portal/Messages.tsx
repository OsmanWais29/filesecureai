
import React from 'react';
import { MessagingSystem } from '@/components/messaging/MessagingSystem';

const Messages = () => {
  return <MessagingSystem isClientView={true} />;
};

export default Messages;
